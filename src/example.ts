// This is just a dummy .ts file to play with lint rules and
// test that the root .eslintrc.json is working as expected.

class Foo {
  public bar: string;

  public constructor() {
    this.bar = 'bar';
  }
}

export default Foo;
